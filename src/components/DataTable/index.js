/**
 * DataTable · 高级数据表格组件统一出口
 *
 * 用法：
 *   import DataTable from '@/components/DataTable'
 *   // 或具名导入
 *   import { DataTable } from '@/components/DataTable'
 *
 * 零外部依赖（仅 React），支持列排序、列级模糊筛选、分页、
 * 行选择、空状态、加载骨架与响应式横向滚动。
 */
export { default, DataTable } from './DataTable.jsx'
